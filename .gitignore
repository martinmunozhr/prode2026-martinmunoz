# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-dry
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Env locales (NUNCA commitear secrets — service role, etc.)
.env
.env.local
.env.*.local
# .env.example SI se commitea (es el template para nuevos devs)
!.env.example

# Panini: los scripts .py se commitean, las imágenes pesadas (scans + crops) no.
scripts/panini/*.jpeg
scripts/panini/*.jpg
scripts/panini/*.png
scripts/panini/crops/
# album_crops: imágenes recortadas de los PDFs (pesadas), pero SÍ commiteamos los _map.json revisados
scripts/panini/album_crops/**/*.jpg
scripts/panini/album_crops/**/*.png

# Config local de MCP (apunta a tu proyecto; no se comparte)
.mcp.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
